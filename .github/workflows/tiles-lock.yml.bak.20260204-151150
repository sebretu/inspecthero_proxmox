name: tiles-lock

on:
  pull_request:
  push:
    branches: [ "main" ]

jobs:
  tiles-lock:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check tiles lock
        run: |
          bash tools/tiles-lock.sh check

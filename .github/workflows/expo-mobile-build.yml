name: Build Expo Mobile (Android & iOS)

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Platform to build'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - android
          - ios
      build_type:
        description: 'Build Type'
        required: true
        default: 'preview'
        type: choice
        options:
          - preview
          - production

jobs:
  build-android:
    if: inputs.platform == 'all' || inputs.platform == 'android'
    runs-on: ubuntu-latest
    steps:
      - name: 🏗️ Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: apps/mobile/package.json

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: 17

      - name: 🐘 Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: 📦 Install Root / Shared Dependencies
        run: |
          npm --prefix packages/sync-protocol install
          npm --prefix apps/mobile install --legacy-peer-deps

      - name: 🎨 Ensure Valid Assets Exist
        run: node apps/mobile/generate_pngs.js

      - name: ⚙️ Prebuild Android Native Project
        working-directory: ./apps/mobile
        run: npx expo prebuild --platform android --clean

      - name: 🔑 Configure Debug Signing for Standalone Release APK
        working-directory: ./apps/mobile/android/app
        run: |
          # Configure release build to use debug signing key so APK is fully standalone without local Metro
          sed -i 's/signingConfig signingConfigs.release/signingConfig signingConfigs.debug/g' build.gradle

      - name: 🔨 Compile Standalone Android APK (Release with Embedded JS Bundle)
        working-directory: ./apps/mobile/android
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --no-daemon

      - name: 📤 Upload Standalone Android APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: et4u-android-standalone-apk
          path: apps/mobile/android/app/build/outputs/apk/release/app-release.apk
          retention-days: 14

  build-ios:
    if: inputs.platform == 'all' || inputs.platform == 'ios'
    runs-on: macos-14
    steps:
      - name: 🏗️ Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: apps/mobile/package.json

      - name: 🍎 Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: 📦 Install Root / Shared Dependencies
        run: |
          npm --prefix packages/sync-protocol install
          npm --prefix apps/mobile install --legacy-peer-deps

      - name: 🎨 Ensure Valid Assets Exist
        run: node apps/mobile/generate_pngs.js

      - name: ⚙️ Prebuild iOS Native Project
        working-directory: ./apps/mobile
        run: npx expo prebuild --platform ios --clean

      - name: 🔨 Compile iOS Simulator Build
        working-directory: ./apps/mobile/ios
        run: |
          pod install
          xcodebuild -workspace et4u.xcworkspace \
                     -scheme et4u \
                     -configuration Debug \
                     -sdk iphonesimulator \
                     -destination 'generic/platform=iOS Simulator' \
                     -derivedDataPath build \
                     CODE_SIGNING_ALLOWED=NO \
                     CODE_SIGNING_REQUIRED=NO \
                     CODE_SIGN_IDENTITY="" \
                     ONLY_ACTIVE_ARCH=NO

      - name: 📦 Package iOS Simulator App & Payload IPA
        working-directory: ./apps/mobile/ios/build/Build/Products/Debug-iphonesimulator
        run: |
          mkdir -p Payload
          cp -r et4u.app Payload/
          zip -r -q et4u-ios-simulator.ipa Payload
          zip -r -q et4u-ios-simulator.zip et4u.app

      - name: 📤 Upload iOS Simulator Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: et4u-ios-simulator-package
          path: |
            apps/mobile/ios/build/Build/Products/Debug-iphonesimulator/et4u-ios-simulator.zip
            apps/mobile/ios/build/Build/Products/Debug-iphonesimulator/et4u-ios-simulator.ipa
          retention-days: 14

"use client";
import UnifiedLayout from "./UnifiedLayout";
export default function UnifiedLayoutClient({ children }: { children: React.ReactNode }) {
  return <UnifiedLayout>{children}</UnifiedLayout>;
}
